version: 2
sphinx:
  configuration: docs/conf.py
python:
  version: 3.8  # we have to use this, 3.9 is not supported for RTD.
  install:
    - method: pip
      extra_requirements:
        - readthedocs
      path: .
